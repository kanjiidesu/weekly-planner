# Updated URL to use the name of the Oracle container (e.g. oracle-db)
spring.datasource.url=jdbc:oracle:thin:@svendeoracle12c:1521:xe

# Testing locally
# spring.datasource.url=jdbc:oracle:thin:@localhost:1541:xe

# Credentials (make sure they match your Oracle container setup)
spring.datasource.username=weeklyplanner
spring.datasource.password=weeklyplanner

# Oracle JDBC driver
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Correct dialect for Hibernate 6
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# App port
server.port=8080
